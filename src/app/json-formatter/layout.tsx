import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JSON Formatter & Validator - Free Online Tool | DevToolBox",
  description: "Free online JSON formatter, validator, and minifier. Beautify JSON with custom indentation. Instant validation with error reporting. No signup required.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
