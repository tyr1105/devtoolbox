import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JWT Decoder - Decode JSON Web Tokens Free | DevToolBox",
  description: "Free online JWT decoder. Inspect JSON Web Token headers, payloads, and expiration status. No signup required.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }

