import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "UUID Generator - Generate UUID v4 Online | DevToolBox",
  description: "Free online UUID v4 generator. Generate one or many UUIDs instantly. Uppercase option available. No signup required.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }

